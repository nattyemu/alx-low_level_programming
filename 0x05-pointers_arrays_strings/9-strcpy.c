#include "main.h"

/**
 * _strcpy - copies the string pointed to by src
 * @dest: destination char input pointer
 * @src: sorce char input pointer
 * Return: nothing
 */

char *_strcpy(char *dest, char *src)
{
int i;
for (i = 0; arc[i] != '\0'; i++)
dest[i] = src[i];
dest[i] = '\0';
return (dest);
}
